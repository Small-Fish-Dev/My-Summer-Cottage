@namespace Sauna.UI
@attribute [StyleSheet]
@using Sauna.Game
@inherits PanelComponent

<root>
	<PauseMenu />
	<InteractionHint />
	<Compass GameTimeManager="@GameTimeManager" />
	<Date />
	<PrimaryTask />
	<Inventory />
	<Crosshair />
</root>

@code {
	public static Hud Instance { get; private set; }

	[Property] public GameTimeManager GameTimeManager;

	public Hud()
	{
		Instance = this;
	}
}
