@namespace Sauna.UI
@attribute [StyleSheet]
@using Sauna.Game
@inherits PanelComponent

<root>
	<SweetMemories /> @* Need to have this on so we can do the rendering. *@
	<FullScreenManager />
</root>

@code {
	public static Hud Instance { get; private set; }

	[Property] public GameTimeManager GameTimeManager;

	public Hud()
	{
		Instance = this;
	}
}
